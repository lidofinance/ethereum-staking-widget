import styled from 'styled-components';
import { Block } from '@lidofinance/lido-ui';

export const StatsWrapperStyle = styled(Block)`
  padding: 0;
`;

export const StatsContentWrapper = styled(Block)`
  display: flex;
  flex-wrap: wrap;
`;
